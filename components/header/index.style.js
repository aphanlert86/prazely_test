import styled from "styled-components";

const HeaderContainer = styled.div`
  padding: 1rem 0;

  .user_image {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    margin-left: auto;
  }
`;

export default HeaderContainer;
